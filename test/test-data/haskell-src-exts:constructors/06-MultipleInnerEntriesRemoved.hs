module MultipleInnerEntriesRemoved where         -- module MultipleInnerEntriesRemoved where
                                                 --
import Language.Haskell.Exts (Exp(Var,Con,Lit))  -- import Language.Haskell.Exts (Exp(    Con)    )
                                                 --
foo = Con                                        -- foo = Con
